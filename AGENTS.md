# Project Custom Instructions

- **Build Configuration**:
  - Always retain `"build": "vite build --base=/power/"` in `package.json` for GitHub Pages deployment.
- **GitHub Actions**:
  - Always maintain `.github/workflows/deploy.yml` for automated GitHub Pages deployment.
